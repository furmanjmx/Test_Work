{
  "name": "example_protractor_booking",
  "version": "1.0.0",
  "description": "setUp_protractor",
  "main": "-",
  "scripts": {
    "webdriver:update": "./node_modules/.bin/webdriver-manager update",
    "webdriver:start": "./node_modules/.bin/webdriver-manager start",
    "setup:e2e": "npm run webdriver:update && npm run webdriver:start",
    "e2e": "./node_modules/.bin/protractor ./protractor.config.js"
  },
  "devDependencies": {
    "protractor": "^5.1.1",
    "webdriver-manager": "^12.0.2"
  }
}
